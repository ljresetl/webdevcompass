# Правила контенту для блогу і новин

Ці правила обов'язкові при додаванні будь-якої нової статті в `src/content/blog/` або новини в `src/content/news/`.

## 1. Структура: картка (короткий текст) → повна сторінка

І блог, і новини працюють за однією схемою: список показує **картку з коротким текстом**, клік по картці веде на **окрему сторінку з повним текстом**.

**Куди саме писати — блог чи новини?** Критерій не "звідки взяли інформацію", а **жанр тексту**:
- **Блог** — вічнозелений (evergreen) навчальний контент: гайди, пояснення "як зробити X", розбори понять. Актуальний однаково і сьогодні, і через рік. Приклад: "Як будувати SEO-посилання" — це блог, навіть якщо в основі лежить свіжа офіційна документація.
- **Новини** — прив'язані до конкретної події з датою: щось відбулось/змінилось (реліз, оновлення політики, вразливість, оновлення самого сайту). Актуальність з часом спадає.
- Якщо вагаєтесь — уявіть заголовок через рік: "Як робити X" читається нормально в будь-який момент (блог); "Google щойно змінив Y" застаріває (новина).

- Блог: `/ua/blog` (картки, `excerpt`) → `/ua/blog/[slug]` (повна стаття, `.mdx`).
- Новини: `/ua/news` (картки на всю ширину, одна під одною, `excerpt`) → `/ua/news/[id]` (повна новина, `.mdx`).
- `excerpt` — короткий тизер для картки (за тестом `content.test.ts` — до 400 символів). Повний текст завжди в окремому `.mdx`-файлі, ніколи в самому `excerpt`.
- Повна сторінка новини — **мінімум 3500 символів по суті, без води** (перевіряється тестом `content.test.ts` по розміру `.mdx`-файлу). Це стосується й новин із зовнішніх джерел (розгорнутий переказ з поясненням контексту й практичних висновків), і власних оновлень сайту.

## 2. Слаг (URL) — українською, не англійською

- `slug` статті блогу і `id` новини (він же URL-адреса, яку бачить людина в браузері: `/ua/blog/<slug>`, `/ua/news/<id>`) пишеться **транслітерованою українською**, а не англійськими словами. Приклад: `skilky-koshtuye-lending`, а не `how-much-does-a-landing-page-cost`.
- Виняток — власні назви й технічні терміни, які не мають сенсу транслітерувати: бренди (`wordpress`, `chrome`, `google`), універсальні абревіатури (`seo`, `api`, `html`, `spa`). Решта слів у слагу — завжди транслітерована українська.
- Транслітерація проста, без офіційного ДСТУ-стандарту, головне — консистентність із уже наявними слагами: `і`→`i`, `и`→`y`, `й`→`i`, `ь`→ (пропускається), `х`→`kh`, `ч`→`ch`, `ш`→`sh`, `щ`→`shch`, `є`→`ye`, `ю`→`yu`, `я`→`ya`.
- Слова розділяються дефісом, усе в нижньому регістрі, без символів крім латинських літер, цифр і дефіса.

## 3. Без дублів

- **Перед тим як додавати нову статтю чи новину — перевірте, чи такої теми/новини вже немає** в `blog/posts.ts` і `news/news.ts` (заголовки, а для новин — ще й `sourceUrl`). Не публікувати одну й ту саму історію двічі, навіть під іншим заголовком чи з іншого джерела.
- Автоматично перевіряється тестом `content.test.ts`, який **валить білд**, якщо: заголовок повторюється всередині блогу, всередині новин, або між блогом і новинами; два записи новин мають однаковий `sourceUrl`.
- Це страхує від очевидних дублів (той самий заголовок/джерело), але не від переказу тієї самої теми іншими словами — це вже потрібно перевіряти самостійно перед додаванням.

## 4. Фото

- Кожна стаття блогу і кожна новина повинні мати **мінімум 3 зображення**. Поле `images: ContentImage[]` (тип експортується з `src/content/blog/posts.ts`) обов'язкове; при білді (`posts.ts` / `news.ts`) є перевірка, яка **кидає помилку**, якщо зображень менше трьох.
- `images[0]` — обкладинка (картка списку, OG-зображення, hero на сторінці). Решта — вставляються всередину тексту `.mdx` (`![alt](url)`).
- Джерело зображень за замовчуванням — Picsum Photos (`https://picsum.photos/seed/<унікальний-seed>/ширина/висота`), безкоштовний сервіс реальних фото без прав на конкретний знімок і без ризику "битого" посилання. Кожен seed — унікальний.
- Якщо є конкретне тематичне фото з Unsplash/Pexels під ліцензією, що дозволяє вільне використання, — можна замість Picsum.
- Завжди з `alt`-текстом, що описує зміст фото.

## 5. Тон і стиль тексту

- Текст пишеться **дружньо і розповідно** — як людина розповідає колезі, а не як офіційний прес-реліз.
- Речення короткі, без канцеляриту. Абзаци не довші за 3-4 речення, розбивайте довгий текст підзаголовками (`## ...`).
- Мета — щоб читач дочитав до кінця. Конкретні приклади, цифри, практична користь, без "води" — особливо важливо для новин на 3500+ символів, де легко скотитись у переливання з пустого в порожнє.
- Кожна стаття блогу завершується м'яким CTA-посиланням на релевантну сторінку послуги (`/ua/services/...`).

## 6. Джерела для новин

- Новини беруться **тільки з офіційних джерел** — як англомовних міжнародних (офіційні блоги Google, Chrome for Developers, W3C, MDN, визнані галузеві видання), так і **українських** (напр. DOU.ua, AIN.ua, itc.ua, офіційні заяви українських компаній/держорганів у сфері IT) — не анонімні агрегатори чи чутки.
- При пошуку новин перевіряйте **обидва** типи джерел — і міжнародні англомовні, і українські — а не лише одні.
- Джерела мають бути **різні** — не використовувати одне й те саме видання щоразу, чергувати.
- Оригінал перекладається українською і **переписується оригінальним текстом** (переказ своїми словами, а не дослівний переклад) — щоб уникнути дублю контенту й плагіату.
- Кожна новина із зовнішнього джерела має посилання на першоджерело (`sourceUrl` у `news.ts`, рендериться на сторінці новини під текстом). Це посилання завжди з **`rel="nofollow"`** (налаштовано в `[id]/page.tsx` і в `mdx-components.tsx` для всіх зовнішніх посилань у контенті) — сайт не передає вагу стороннім ресурсам.

## 7. TODO: лічильник переглядів ("оченятко")

Поки не реалізовано — прибрано з UI 2026-09-12, бо без бази даних лічильник був би фейковою заглушкою (`views: number` вручну), а не реальними переглядами живих людей. Не додавайте назад без реального підрахунку.

Коли на сайті з'явиться сховище даних (Vercel KV / Upstash тощо):
1. Підключити інтеграцію в Vercel Dashboard → Storage, додати env-змінні.
2. Створити Route Handler (напр. `/api/views/[id]`), який інкрементує лічильник для конкретної новини/статті при завантаженні сторінки (не при prefetch) і за можливості фільтрує ботів за User-Agent.
3. Показати іконку ока + число на картці (`News.tsx`) і на сторінці (`[id]/page.tsx`) — раніше вже була верстка з іконкою ока (SVG `M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z` + `circle cx="12" cy="12" r="3"`), можна відновити той самий вигляд.

## 8. Поділитися

- Обидва типи сторінок (`/ua/blog/[slug]` і `/ua/news/[id]`) мають блок `<ShareButtons>` (`src/components/Share/ShareButtons.tsx`) — Telegram, X, LinkedIn і копіювання посилання. Додавайте його на будь-яку нову сторінку повної статті/новини.

## Як додати новий запис

**Новина** — додати об'єкт **на початок** масиву `newsItems` у `src/content/news/news.ts` (`id`, `date`, `tag`, `title`, `excerpt`, `images` мінімум 3, `sourceUrl` якщо зовнішня) + `.mdx`-файл `src/content/news/<id>.mdx` з повним текстом (мінімум 3500 символів).

**Стаття блогу** — додати `.mdx` файл у `src/content/blog/` + запис у `src/content/blog/posts.ts` (`slug`, `title`, `excerpt`, `date`, `images` мінімум 3).

Нові URL (включно зі сторінками окремих новин) автоматично потрапляють у `sitemap.xml` і надсилаються в IndexNow після наступного продакшн-білду — нічого додатково налаштовувати не треба.
